version: '1'
name: debug
vars:
  URL: "https://example.com"
tasks: 
  b:
    schedule: "@every 2s"
    timezone: "Asia/Ho_Chi_Minh"
    executor: http
    clients: [client2]
    payload: |
      {
        "method": "GET",
        "url": "{{.URL}}",
        "headers": "[]",
        "body": "",
        "timeout": "30",
        "expectCode": "200",
        "expectBody": ""
      }

brokers:
  brok:
    listens: [a_task]
    flows: [client2_client]